apiVersion: v1
kind: Service

metadata:
  name: springboot-service
  namespace: application

spec:
  type: ClusterIP

  selector:
    app: springboot
    version: green

  ports:
  - port: 80
    targetPort: 80